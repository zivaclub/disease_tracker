{
  "name": "react-example",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:legacy": "tsx server.ts",
    "dev:backend": "cd backend && uvicorn app.main:app --reload --port 8000",
    "dev:db": "docker compose up -d db redis",
    "dev:full": "npm run dev:db && sleep 3 && npm run dev:backend & npm run dev",
    "start:docker": "docker compose up --build",
    "start:docker:detach": "docker compose up -d --build",
    "stop:docker": "docker compose down",
    "build:frontend": "vite build",
    "build": "npm run build:frontend",
    "build:legacy": "vite build && esbuild server.ts --bundle --platform=node --format=cjs --packages=external --sourcemap --outfile=dist/server.cjs",
    "start": "cd backend && uvicorn app.main:app --host 0.0.0.0 --port 8000",
    "start:legacy": "node dist/server.cjs",
    "lint": "tsc --noEmit",
    "test:backend": "cd backend && pytest",
    "seed": "cd backend && python scripts/seed_db.py",
    "validate-sources": "cd backend && python scripts/validate_sources.py"
  },
  "dependencies": {
    "@google/genai": "^2.4.0",
    "@tailwindcss/vite": "^4.1.14",
    "@vitejs/plugin-react": "^5.0.4",
    "lucide-react": "^0.546.0",
    "react": "^19.0.1",
    "react-dom": "^19.0.1",
    "vite": "^6.2.3",
    "express": "^4.21.2",
    "dotenv": "^17.2.3",
    "motion": "^12.23.24"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "autoprefixer": "^10.4.21",
    "esbuild": "^0.25.0",
    "tailwindcss": "^4.1.14",
    "tsx": "^4.21.0",
    "typescript": "~5.8.2",
    "vite": "^6.2.3",
    "@types/express": "^4.17.21"
  }
}
